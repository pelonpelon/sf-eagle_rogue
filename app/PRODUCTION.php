        //TODOp ••• set default cat to event
        //TODOp ••• fix somethings fucked up
        //TODOp ••• fix media dimensions in wp LIVE +1000
        //TODOp ••• categories: news, events
        //TODOp ••• add social media
        //TODOp ••• add pages to wp
        //TODOp  install plugins: metabox
        //TODOd ••• rename staff2 def2 staff-postcard staff-profile
        //TODOd ••• cron removes poster if generic exists
        //TODOd ••• cron duplicate only required stuff on repeat
        //TODOd ••• add Sponsors,beneficiaries with hosts, guests
        //TODOd ••• 404.html not working
        //TODOd •• when things expire chng to draft
        //TODOd •• staff button simplify classes
        //TODOd •• row colors should be random so they don't repeat, code?
        //TODOd •• put menu-trigger on own layer for animation
        //TODOd •• make useable post template so I can preview both post and postcard
        //TODOd •• generics could have a post type or category
        //TODOd •• editor should add <br> for blanks
        //TODOd •• all defaults, print known details fill in shortcode for others
        //TODOd •• add Error Image for get_image_url_by_slug
        //TODOd •• Try a video on popup
        //TODOd • there are Woof pics
        //TODOd • should staff title default to left
        //TODOd • lightbox:413,409 else #jslghtbx exists (add class)
        //TODOd • lightbox submit changes "off" and ?prevent default?
        //TODOd • jPanelMenu submit changes on jank
        //TODOd • create Softball post_type
        //TODOd • cancel default on modal clicks when you don't want to leave (class?)
        //TODOd • band page, add multiple [links] after name
        //TODOd • TNL icon should have trans bg
        //TODOd • Info page of pages with menu at top
        //TODOd • djs urls
        //TODOd  rename some of those function files
        //TODOd  multicolored bouncing lightbox balls
        //TODOd  make rows of djs and guests colorful
        //TODOd  lanscape pages scroll with scroller
        //TODOd  gradient not as prominent as hoped (in light on iphone)
        //TODOd  generic default url
        //TODOd  def need notes box
        //TODOd  add "Generic image" url box
        //TODOd  Staff needs edit buttons
        //TODOd  Logo modal: [page title=''] HOT
        //FIXX  rows should replace space with nbsp to keep names together (but some are long)
        //FIXX  subtitle should be any length
        //FIXX  staff default
        //FIXX  news title not centered
        //FIXX  news needs an expiration
        //FIXX  news links must have color
        //FIXX  max posts for staff set at 10
        //FIXX  in postcard, h4 same color as subtitle, if I want to use both?
        //FIXX  colors too dark, maybe not fade posters on laptop
        //FIXX  [who id='hosts' fmt='row'] shows up even when empty
        //FIXX  Loading is very ugly
        //FIXX  <p> on pages needs margins
        //FIXX  pages get rid of margins horiz, you want to see the most
        //FIXX  click on news link, ignore default
        //FIXX  (This Thing) becomes (this Thing)
        //FIXX ••• remove feature img etc upon weekly/monthly if generic
        //FIXX ••• does godaddy run cron?
        //FIXX no margins for p in postcard and litte on h
        //FIXX bring cover and time closer together
        //FIXX ••• Ack internal FIXXs
        //TODOd ••• shortcode for formatted social
        //TODOd • is forcing lowercase too much on who, TomAss doesn't work




